# 실무에서 인증/인가 처리 방식

## 📌 인증(Authentication) vs 인가(Authorization)

### 인증 (Authentication)
- **질문**: "당신은 누구인가?"
- **목적**: 사용자가 로그인했는지 확인
- **처리 위치**: 인터셉터/필터 (현재 구현 ✅)

### 인가 (Authorization)  
- **질문**: "당신이 이 작업을 할 권한이 있나?"
- **목적**: 사용자가 특정 리소스에 접근할 권한이 있는지 확인
- **처리 위치**: 여러 방법 존재

---

## 🔍 실무에서 인가 처리 방법

### 1️⃣ **컨트롤러에서 직접 처리** (현재 코드 방식)
```java
@PostMapping("/board/{id}/update")
public String updateProc(@PathVariable Long id, ...) {
    User sessionUser = (User) session.getAttribute("sessionUser");
    
    Board board = repository.findById(id);
    if(!board.isOwner(sessionUser.getId())) {
        throw new Exception403("권한 없음");
    }
    // ... 수정 로직
}
```

**장점:**
- ✅ 비즈니스 로직이 명확함
- ✅ 리소스 기반 권한 검사에 적합 (게시글 소유자 확인 등)
- ✅ 복잡한 권한 로직 처리 용이

**단점:**
- ❌ 컨트롤러 코드가 길어질 수 있음
- ❌ 권한 검사 로직이 중복될 수 있음

**언제 사용?**
- 리소스 기반 권한 검사 (게시글 소유자, 댓글 작성자 등)
- 복잡한 비즈니스 로직이 필요한 경우

---

### 2️⃣ **인터셉터로 처리** (예시 코드 제공)
```java
@Component
public class OwnerCheckInterceptor implements HandlerInterceptor {
    @Override
    public boolean preHandle(...) {
        // URL 패턴에서 ID 추출
        // 권한 검사 수행
    }
}
```

**장점:**
- ✅ 컨트롤러 코드가 깔끔해짐
- ✅ 권한 검사 로직이 중앙화됨
- ✅ URL 패턴 기반 권한 검사에 적합

**단점:**
- ❌ URL 패턴에 의존적
- ❌ 복잡한 비즈니스 로직 처리 어려움
- ❌ 리소스 조회가 인터셉터에서 발생 (성능 고려 필요)

**언제 사용?**
- URL 패턴 기반 권한 검사 (`/admin/**`는 관리자만)
- 단순한 권한 검사

---

### 3️⃣ **AOP + 어노테이션 기반** (Spring Security 사용 시)
```java
@PreAuthorize("hasRole('ADMIN')")
@GetMapping("/admin/users")
public String adminPage() { ... }

@PreAuthorize("@boardService.isOwner(#id, authentication.name)")
@PostMapping("/board/{id}/update")
public String update(@PathVariable Long id) { ... }
```

**장점:**
- ✅ 선언적 프로그래밍 (코드가 매우 깔끔)
- ✅ 메서드 레벨 권한 제어 가능
- ✅ Spring Security와 통합 용이

**단점:**
- ❌ Spring Security 의존성 필요
- ❌ 학습 곡선 존재

**언제 사용?**
- Spring Security를 사용하는 프로젝트
- 역할(Role) 기반 권한 관리
- 복잡한 권한 정책이 필요한 대규모 프로젝트

---

## 🎯 현재 프로젝트 권장 방식

### 현재 코드 (컨트롤러에서 처리) ✅ **추천**

**이유:**
1. **리소스 기반 권한 검사**: 게시글 소유자 확인은 리소스(게시글)를 조회해야 함
2. **명확성**: 컨트롤러에서 바로 확인 가능
3. **유연성**: 복잡한 비즈니스 로직 처리 용이

```java
// 현재 방식 (권장)
@PostMapping("/board/{id}/update")
public String updateProc(@PathVariable Long id, ...) {
    User sessionUser = (User) session.getAttribute("sessionUser");
    
    // 리소스 조회 + 권한 검사
    Board board = repository.findById(id);
    if(!board.isOwner(sessionUser.getId())) {
        throw new Exception403("권한 없음");
    }
    
    // 수정 로직
    repository.updateById(id, updateDTO);
}
```

---

## 📊 비교표

| 방식 | 장점 | 단점 | 적합한 경우 |
|------|------|------|------------|
| **컨트롤러 처리** | 명확, 유연 | 코드 중복 가능 | 리소스 기반 권한 검사 |
| **인터셉터** | 중앙화, 깔끔 | URL 패턴 의존 | URL 패턴 기반 권한 검사 |
| **AOP/어노테이션** | 매우 깔끔, 선언적 | Spring Security 필요 | 역할 기반, 대규모 프로젝트 |

---

## 💡 실무 팁

1. **인증은 항상 인터셉터/필터로 처리** (현재 구현 ✅)
2. **인가는 상황에 따라 선택**:
   - 단순한 URL 패턴 기반 → 인터셉터
   - 리소스 기반 (소유자 확인) → 컨트롤러
   - 역할 기반 + 대규모 → Spring Security + AOP

3. **현재 프로젝트는 컨트롤러 방식이 적합** ✅
   - 게시글 소유자 확인은 리소스 기반
   - 코드가 명확하고 이해하기 쉬움
   - 학습 목적에도 적합

